{
  "version": 2,
  "buildCommand": "npm run build:ui",
  "outputDirectory": "dist/ui",
  "routes": [{ "handle": "filesystem" }, { "src": "/(.*)", "dest": "/" }],
  "framework": "vite",
  "github": {
    "silent": true
  }
}
